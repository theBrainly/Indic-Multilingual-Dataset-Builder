# Core (light, mostly pure-python) — enough to run the full demo offline.
pydantic>=2.0
pyyaml>=6.0
datasketch>=1.6
regex>=2023.0
pandas>=1.5
pyarrow>=12.0
numpy>=1.23
tqdm>=4.65

# Optional / heavy — install for production-grade semantic dedup & live scraping:
#   pip install -e ".[full]"
# sentence-transformers>=2.2
# faiss-cpu>=1.7
# beautifulsoup4>=4.12
# requests>=2.31
# datasets>=2.14
# scrapy>=2.11
